{
  "name": "mahjongsoul-data-overlay",
  "version": "2026.4.5",
  "private": true,
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "electron:start:launch": "electron electron/launch-main.js",
    "electron:start:collect": "electron electron/collect-main.js",
    "build:installer:launch": "node build-electron-installer.mjs launch",
    "build:installer:collect": "node build-electron-installer.mjs collect",
    "build:installer": "node build-electron-installer.mjs all",
    "build:electron:launch": "node build-electron.mjs launch",
    "build:electron:collect": "node build-electron.mjs collect",
    "build:electron": "node build-electron.mjs all"
  },
  "dependencies": {
    "playwright": "^1.59.1"
  },
  "devDependencies": {
    "@electron/packager": "^19.1.0",
    "electron": "^37.6.0",
    "electron-builder": "^26.8.1",
    "esbuild": "^0.28.0",
    "pkg": "^5.8.1"
  },
  "pkg": {
    "assets": [
      "overlay/**/*"
    ]
  }
}
